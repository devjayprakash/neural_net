export default class SoftMaxLayer {
  outputs: number[];

  constructor(outputs: number[]) {
    this.outputs = outputs;
  }

  getOutput(inputs: number[]) {
    let output: number[] = [];

    return output;
  }
}
